# GoDaddy DNS 配置 - fishtalk.app 部署到 Vercel

## 🎯 配置目标
将 GoDaddy 购买的域名 `fishtalk.app` 指向 Vercel 托管的网站

---

## 📋 准备工作

1. ✅ GoDaddy 账号和域名 `fishtalk.app`
2. ✅ Vercel 账号和已部署的项目
3. ✅ 确保域名未过期且处于活动状态

---

## 🚀 方法一：使用 Vercel Nameservers（推荐 - 最简单）

### 第 1 步：在 Vercel 获取 Nameservers

1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)
2. 选择您的项目（fish_art_2 / fishtalk 分支）
3. 点击顶部 **"Settings"** 标签
4. 左侧菜单选择 **"Domains"**
5. 点击 **"Add"** 按钮
6. 输入 `fishtalk.app`，点击 **"Add"**
7. 选择 **"Use Vercel Nameservers"**

Vercel 会显示两个 nameserver 地址：
```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

**📝 记下这两个地址**

### 第 2 步：登录 GoDaddy 修改 Nameservers

1. 访问 [GoDaddy](https://www.godaddy.com/) 并登录
2. 点击右上角您的用户名
3. 选择 **"My Products"** (我的产品)
4. 找到 **"Domains"** (域名) 部分

### 第 3 步：修改域名的 Nameservers

1. 找到 `fishtalk.app` 域名
2. 点击域名右侧的 **"DNS"** 按钮（或点击域名进入管理页面）
3. 在域名设置页面，找到 **"Nameservers"** 部分
4. 点击 **"Change"** (更改) 按钮

### 第 4 步：输入 Vercel 的 Nameservers

1. 选择 **"I'll use my own nameservers"** (使用自定义名称服务器)
2. 输入 Vercel 提供的 nameservers：
   ```
   ns1.vercel-dns.com
   ns2.vercel-dns.com
   ```
3. 点击 **"Save"** (保存)

### 第 5 步：确认修改

GoDaddy 会显示警告说明更改 nameservers 的影响：
- ⚠️ 现有的 DNS 记录将不再生效
- ⚠️ 电子邮件服务可能受影响（如果有）

确认无误后，点击 **"Continue"** 继续

### 第 6 步：等待生效

- ⏰ **通常需要**: 5-30 分钟（GoDaddy 较快）
- 🕐 **最长可能**: 48 小时
- ✅ Vercel 会自动：
  - 配置所有必要的 DNS 记录
  - 签发 SSL 证书
  - 设置 www 重定向

### 第 7 步：验证配置

在 Vercel Dashboard 的 Domains 页面：
- 等待域名状态变为 ✅ **"Valid Configuration"**
- SSL 证书状态显示 ✅ **"Active"**

---

## 🔧 方法二：保留 GoDaddy DNS，手动添加记录

如果您想继续使用 GoDaddy 的 DNS（例如还有邮件服务），可以手动配置。

### 第 1 步：在 Vercel 添加域名

1. Vercel Dashboard → Settings → Domains
2. 添加 `fishtalk.app`
3. 这次选择 **"Add DNS Records"** (添加 DNS 记录)
4. Vercel 会显示需要添加的具体记录

### 第 2 步：登录 GoDaddy DNS 管理

1. GoDaddy → My Products → Domains
2. 找到 `fishtalk.app`
3. 点击 **"DNS"** 按钮
4. 您会看到 DNS 管理面板

### 第 3 步：添加 A 记录（主域名）

在 DNS 管理页面：

1. 向下滚动到 **"Records"** (记录) 部分
2. 点击 **"Add"** (添加) 按钮
3. 填写以下信息：

```
Type (类型):       A
Name (名称):       @
Value (值):        76.76.21.21
TTL:              1 Hour (或 Custom: 600 seconds)
```

4. 点击 **"Save"** (保存)

### 第 4 步：添加 CNAME 记录（www 子域名）

1. 再次点击 **"Add"** 按钮
2. 填写以下信息：

```
Type (类型):       CNAME
Name (名称):       www
Value (值):        cname.vercel-dns.com
TTL:              1 Hour
```

**⚠️ 重要**: 
- Value 值末尾不要有点号
- 确保输入 `cname.vercel-dns.com` 而不是 `cname.vercel-dns.com.`

3. 点击 **"Save"**

### 第 5 步：删除冲突记录（如果有）

检查是否有以下冲突记录需要删除：

- ❌ 名称为 `@` 的旧 A 记录（指向其他 IP）
- ❌ 名称为 `@` 的 CNAME 记录
- ❌ 名称为 `www` 的旧 A 记录
- ❌ GoDaddy 的 Parked (停放) 记录

如有，点击记录右侧的 **垃圾桶图标** 或 **"Delete"** 删除

### 第 6 步：添加 TXT 验证记录（如 Vercel 要求）

如果 Vercel 显示需要验证域名所有权：

1. 点击 **"Add"** 按钮
2. 填写：

```
Type:              TXT
Name:              _vercel
Value:             [从 Vercel 复制的验证码]
TTL:               1 Hour
```

3. 保存后返回 Vercel，点击 **"Verify"** (验证)

### 第 7 步：配置总结

完成后，您的 DNS 记录应该包括：

| Type | Name | Value | TTL |
|------|------|-------|-----|
| A | @ | 76.76.21.21 | 1 Hour |
| CNAME | www | cname.vercel-dns.com | 1 Hour |
| TXT | _vercel | [验证码] | 1 Hour |

---

## 🔍 验证 DNS 配置

### 方法 1: 使用 GoDaddy 的工具

GoDaddy DNS 页面底部有 **"Test your site"** 功能，可以测试域名解析。

### 方法 2: 在线检查工具

访问 [DNSChecker.org](https://dnschecker.org/)：
1. 输入 `fishtalk.app`
2. 选择 **"A"** 记录类型
3. 查看全球不同地区的解析结果
4. 应该显示 `76.76.21.21`

对 `www.fishtalk.app` 重复上述步骤：
1. 选择 **"CNAME"** 记录类型
2. 应该显示 `cname.vercel-dns.com`

### 方法 3: 命令行验证

打开 PowerShell 或命令提示符：

```powershell
# 检查主域名
nslookup fishtalk.app

# 检查 www 子域名
nslookup www.fishtalk.app

# 使用 Google DNS 检查（确保不是缓存）
nslookup fishtalk.app 8.8.8.8
```

正确的输出应该是：
```
Server:  dns.google
Address:  8.8.8.8

Name:    fishtalk.app
Address:  76.76.21.21
```

---

## 🧹 清除 DNS 缓存（如需要）

如果修改后无法立即访问，清除本地缓存：

**Windows**:
```powershell
ipconfig /flushdns
```

**Chrome 浏览器**:
1. 访问: `chrome://net-internals/#dns`
2. 点击 **"Clear host cache"**

---

## ⏱️ 生效时间

| 操作 | GoDaddy 通常时间 |
|------|-----------------|
| 修改 DNS 记录 | 5-15 分钟 |
| 更改 Nameservers | 10-60 分钟 |
| 全球传播 | 最长 48 小时 |

GoDaddy 的 DNS 更新通常比较快！

---

## ❗ 常见问题

### 问题 1: 修改 Nameservers 后邮箱不能用了

**原因**: 更改到 Vercel nameservers 后，原有的 MX（邮件）记录失效

**解决方案**:
1. 在 Vercel Dashboard → Domains → 点击域名
2. 添加您的 MX 记录（从原 GoDaddy DNS 复制）
3. 或使用方法二保留 GoDaddy DNS

### 问题 2: 显示 "DNS_PROBE_FINISHED_NXDOMAIN"

**解决**:
1. 确认 DNS 记录已正确添加
2. 等待 10-30 分钟让 DNS 传播
3. 清除浏览器和系统 DNS 缓存
4. 尝试用手机 4G 网络访问（排除本地缓存）

### 问题 3: GoDaddy 显示 "Forwarding" 或 "Parked"

**解决**:
1. 进入域名的 **"Settings"** (设置)
2. 找到 **"Domain Forwarding"** 或 **"Parking"**
3. 点击 **"Manage"** → 删除转发设置
4. 然后重新配置 DNS

### 问题 4: 证书错误或不安全

**解决**:
1. 等待 5-10 分钟，Vercel 需要时间签发证书
2. 在 Vercel → Domains → 点击 **"Refresh"** 刷新证书
3. 确保 DNS 解析正确（使用 dnschecker.org 验证）

### 问题 5: www 可以访问但主域名不行（或反之）

**解决**:
1. 确保同时配置了 A 记录（@）和 CNAME 记录（www）
2. 在 Vercel 中同时添加两个域名：
   - `fishtalk.app`
   - `www.fishtalk.app`

---

## 📧 保留 GoDaddy 邮箱服务

如果您使用 GoDaddy 邮箱或 Microsoft 365：

### 如果使用方法一（Vercel Nameservers）:

需要在 Vercel DNS 中添加邮箱记录：

1. Vercel → Settings → Domains → 点击域名
2. 添加 MX 记录（从 GoDaddy 获取）

**GoDaddy 邮箱常用 MX 记录**:
```
Priority: 0
Value: mailstore1.secureserver.net
```

### 如果使用方法二（保留 GoDaddy DNS）:

无需操作，邮箱记录会继续生效！

---

## ✅ 最终检查清单

配置完成后，请确认：

- [ ] 访问 `https://fishtalk.app` 能打开网站
- [ ] 访问 `https://www.fishtalk.app` 能打开网站
- [ ] 地址栏显示 🔒 HTTPS 安全锁
- [ ] 页面标题显示 "Draw a Fish Online - Free AI Fish Drawing Tool | fishtalk.app"
- [ ] 页面导航栏显示 "🐟 FishTalk AI"
- [ ] 页面样式正常，无布局错误
- [ ] 画布和功能正常工作
- [ ] 打开 F12 开发者工具，Console 无红色错误

---

## 📞 需要帮助？

### GoDaddy 支持
- **帮助中心**: https://www.godaddy.com/help
- **在线聊天**: 登录后右下角聊天图标
- **电话**: 查看 GoDaddy 网站的联系页面

### Vercel 支持
- **文档**: https://vercel.com/docs/concepts/projects/domains
- **社区**: https://github.com/vercel/vercel/discussions

---

## 🎓 相关资源

- [GoDaddy - 更改域名服务器](https://www.godaddy.com/help/change-nameservers-for-my-domains-664)
- [GoDaddy - 管理 DNS](https://www.godaddy.com/help/manage-dns-680)
- [Vercel - 添加域名](https://vercel.com/docs/concepts/projects/domains/add-a-domain)

---

**配置指南版本**: 1.0 - GoDaddy 专版  
**更新日期**: 2025-11-07  
**域名**: fishtalk.app  
**注册商**: GoDaddy  
**托管平台**: Vercel

祝配置顺利！🚀




